<%= form_with model: [@pet, @note], local: true do |f| %>
  <% if note.error.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(note.errors.count, "error") %> prohibited this note from being saved:</h2>

      <ul>
      <% note.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.hidden_field :pet_id, value: @pet.id %>

  <div>
    <%= f.label :body %>
    <%= f.text_area :body %>
  </div>

  <div>
    <%= f.label :datetime %>
    <%= f.text_field :datetime, value: Time.zone.now.strftime("%Y/%m/%d %X") %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
